import {writable} from 'svelte/store'

// Global rdkit reference
export const rdkit = writable(null)

// Global chemdoodle references
export const chemdoodle = writable(null)
export const jquery = writable(null)
